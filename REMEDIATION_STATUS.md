# Tandem Remediation Status

Branch: `remediation/audit-fixes`  
Source audit: `AUDIT_REPORT.md` at revision `c0eb1ff`  
Initial audit verdict: **32/100 - NOT READY - DEMO**  
Status date: 2026-09-11

This is the authoritative remediation ledger for every CRITICAL, HIGH, and MEDIUM finding in the independent audit. A finding is marked `FIXED` only after its regression test and the relevant broader verification commands have run successfully. Commit hashes are recorded after each logical remediation commit.

| ID | Finding | Severity | Audit evidence | Root cause | Planned fix | Regression test | Phase | Implementation status | Verification result | Commit |
|---|---|---|---|---|---|---|---:|---|---|---|
| C-01 | Discovery is fake, not model-driven | CRITICAL | `tandem/discovery/agent.py:44-175`; no provider client; synthetic call counter | Discovery is a fixed Playwright script with fabricated model telemetry | Add a provider abstraction and real provider-backed action loop; persist observations, decisions, actions, results, screenshots, and source run ID; compile only recorded events | Provider contract/transport tests; discovery trace/provenance test; credential-gated real provider run | 1, 18-20 | FIXED | OpenAI Responses transport contract, strict typed decisions, provider-directed browser E2E, durable sanitized cycle evidence, trace-bound compilation/provenance, and zero-model replay all pass; 17/17 impacted tests pass and scoped Ruff/mypy pass | `a79e6fe` |
| C-02 | Two workers can both commit the same effect | CRITICAL | `engine.py:63-112`; `repository.py:229-240`; independent race produced two `COMPLETED` results | Precheck precedes exclusion; lease overwrites; staged intents are ignored; no atomic reservation or fencing | Add canonical full-identity idempotency key, unique durable claim, atomic conditional transitions, monotonic fencing token, and hold claim through reconciliation | True multi-process same-effect race repeated 20-50 times; independently assert one target effect | 1, 3-5 | FIXED | 20 repeated two-process races each produced exactly one independently stored target effect; preserved audit race and atomic admission tests pass | `9c463d8`, `7171391` |
| C-03 | Precheck failure is treated as effect absence | CRITICAL | `tandem/replay/precheck.py:33-54`; outage adversarial test returns `None` | Broad exception handling collapses unavailable/invalid/ambiguous into absence | Introduce typed precheck outcomes; allow COMMIT only after positive `NOT_APPLIED`; halt all unavailable, malformed, timeout, and ambiguous results | 500, timeout, malformed, missing, ambiguous, already-applied, and absent precheck cases | 1, 6 | FIXED | 7 focused checks passed; timeout/transport, HTTP 500, malformed JSON, missing identity, mismatch all halt and executor is not invoked | `e175796` |
| C-04 | Guard fails open and omits case binding | CRITICAL | `executor.py:81-100`; `guards.py:32-43`; `expected_case_template` unused; wrong case posted | Observations are optional and case/account/currency are not structurally bound to actuation | Require complete effect identity and exact owning-form evidence immediately before mutation; missing or ambiguous evidence fails closed | Missing member/account/case/amount/currency evidence and wrong case/member/account/currency tests | 1, 3, 8-9 | FIXED | Exact owning-form submission fails closed for all six identity fields; every COMMIT `SUBMIT` actuation must structurally reference its guard | `2230cda`, `9294b73` |
| C-05 | Displayed amount can differ from submitted amount | CRITICAL | Visible `$340` with hidden `$34` returned success and posted `$34` | Guard reads display/data attributes rather than submitted controls; postcheck does not validate full identity | Trace mutating control to owning form, bind submitted amount/currency to immutable identity, and verify target-side amount after submit | Visible-vs-hidden amount mismatch and postcheck amount mismatch tests | 1-3, 7-9 | FIXED | Preserved hidden-value audit test plus full six-field form mutation matrix pass; postcheck independently validates target case/member/account/amount | `1aef16c`, `2230cda` |
| C-06 | Core ambiguous-effect reconciliation is unreachable | CRITICAL | `executor.py:226-231` converts browser exceptions to `HARD_FAILURE`; engine reconciliation normally sees only mocked exceptions | Executor discards execution phase and whether submit may have reached the target | Preserve `BEFORE_SUBMIT`, `SUBMIT_INITIATED`, `SUBMIT_CONFIRMED`, and `AFTER_SUBMIT_UNKNOWN`; reconcile by immutable business reference; never blindly retry uncertainty | Real browser path where target commits and response disappears; unavailable/ambiguous reconciliation tests | 1, 7, 10, 12-13 | FIXED | Real browser response-loss tests prove exact target reconciliation to `CONFIRMED_APPLIED` or `UNCERTAIN_EFFECT`; an uncertain claim fences subsequent execution without duplicate credit | `1aef16c`, `dc104e2` |
| C-07 | Crash safety has a durable notice-deadline gap | CRITICAL | `reg_e.py:249-253`; `os._exit(86)` after credit leaves no `NOTICE_2_DAY` deadline | Notice obligation is written after irreversible credit with no durable predecessor from which to rebuild it | Persist obligation before credit; append immutable lifecycle events; reconstruct projections from durable facts during recovery | Process-death matrix before/after obligation, claim, guard, submit, postcheck, final event, notice; assert target and ledger state | 1, 13-14, 16-17 | FIXED | Twelve A-L `os._exit(86)` cases each resume in a fresh process with one credit, one notice, and one `SATISFIED` obligation; money never exists without the durable obligation | `03287c3` |
| C-08 | Target effects are volatile and resume trusts the ledger | CRITICAL | `simulators/*/state.py`; simulator restart erased credit while workflow returned success | External simulator state is process memory and recovery never reconciles target against ledger | Give core, processor, and documents independent SQLite stores; persist effect history; reconcile every prior external effect on resume | Independent simulator restarts; ledger-applied/target-absent divergence; persistence and reconciliation tests | 1, 11-13 | FIXED | Three independent SQLite targets survive spawned-process restart; resume verifies all observable claim identity fields, effect count, and target reference, routing absence/mismatch to `TARGET_LEDGER_DIVERGENCE` | `7dd99e8`, `fb1df6f` |
| H-01 | Processor and notice COMMITs bypass the effect protocol | HIGH | `reg_e.py:136-198`, `272-300` issue direct HTTP calls | Workflow implements ad hoc side-effect handling instead of orchestrating capabilities | Define processor and document COMMIT capabilities and adapters; route claim/precheck/policy/actuation/postcheck/reconciliation through one effect engine | `tests/regression/test_effect_protocol_unification.py` (13 cases: protocol trail, serial duplicate, precheck outage, apply-then-504, session expiry retry, inquiry-unavailable uncertainty fence, notice 500 confirmed-absent retry, pre-submit interstitial retry, 5x two-process processor race) plus crash-matrix rows L-R | 1, 15 | FIXED | 14/14 unified-protocol/handoff tests passed; all 17 hard-crash rows passed in fresh-process groups; 9/9 legacy crash, uncertainty, effect-protocol, and workflow tests passed. Processor and notice now use the same claimed, guarded, reconciled, fenced protocol as core. | `e3cc64d` |
| H-02 | Artifact integrity is cosmetic | HIGH | `compiler.py:115-124`; `capability.py:125-132`; tampered selector loads | Hash is an ignored comment and loader replaces rather than verifies it | Define canonical JSON payload, embed digest outside hashed fields, verify digest/schema version/allowlisted surfaces and targets on load | Tamper selector, URL, effect class, bound, and version; unsupported future version | 1, 20-21 | FIXED | Embedded canonical SHA-256 covers every normalized artifact field except itself; loader rejects missing/mismatched digests, unsupported schema versions, unsupported surfaces, and external navigation targets even with a recomputed digest; all 18 focused tests and 9/10 audit tests passed (sole remaining audit failure is Phase 23 lease ownership) | `be69609` |
| H-03 | COMMIT schema permits non-executable/unguarded artifacts | HIGH | Empty COMMIT validates; executor guard depends on `ASSERT_CONTAINER` or label `Commit Button` | Artifact validation does not encode COMMIT safety topology | Require full effect identity, precheck, postcheck, reconciliation, non-empty steps, mutating actuation, and an immediately adjacent structural guard | Empty/no-actuation/no-guard/non-adjacent/no-precheck/no-postcheck/no-reconciliation/no-identity artifacts | 1, 9, 20 | FIXED | Nine invalid topology variants are rejected; runtime safety keys off typed `SUBMIT` plus matching `guard_ref`, independent of human labels | `9294b73` |
| H-04 | Human ownership is not enforced | HIGH | Engine overwrites active human lease; no expiry or fencing | Lease acquisition is unconditional upsert and browser actions do not validate owner token | Atomic compare-and-swap leases with owner type/ID, TTL, heartbeat, version, fencing; validate token before every action | Human blocks automation, stale token rejected, two humans race, stale/dead lease recovery | 1, 23 | FIXED | SQLite conflict-safe acquisition admits one of two human processes; active human ownership rejects automation; expired takeover increments the fence and rejects the old token; replay validates/heartbeats the lease before every browser action; dead process recovery releases only the exact old owner/token | `6a8cadb` |
| H-05 | Same-session handoff is not an operator feature | HIGH | `handoff/coordinator.py:73-115`; direct in-process `Page`; `browser_session_id` never populated | No durable browser-session broker or operator action transport exists | Browser worker owns context; durable session ID; authenticated fenced operator control/action API acts on same page and resumes automation | Cookie/context continuity, operator action on same page, automation blocked during ownership, resume afterward | 1, 23-24 | FIXED | A dedicated worker thread exclusively owns Playwright; the brokered operator API validates the lease fence and executes bounded actions on that page; E2E proves one worker/thread ID, cookie continuity, stale automation rejection, operator sign-off, resumed automation, durable session record, screenshot, and populated execution session ID. API authentication is tracked separately by H-10/Phase 30. | `ffd9650` |
| H-06 | Documented launcher and handoff demo are incompatible | HIGH | Separate `start_services.py` plus handoff demo times out; demo mutates local singleton | Demo reaches imported process-local test state instead of the running service | Add authenticated simulator/admin control APIs or launch-scoped fixtures; remove test-module imports; exercise brokered handoff across processes | README launcher plus handoff demo subprocess test | 1, 24, 30, 38-39 | PLANNED | Baseline pending | - |
| H-07 | No operational automation service exists | HIGH | Launcher starts only simulators/dashboard; API has no case/run route | Workflow can only be invoked from tests/demo | Add authenticated case creation/run API and worker startup recovery scan; queue uncertainty for reconciliation/human review | API-created case reaches durable terminal state; restart resumes recoverable work but not uncertain COMMIT | 1, 29-30 | PLANNED | Baseline pending | - |
| H-08 | Beta is not a separate institution and artifact is mutated | HIGH | Same app/state; demo/tests deep-copy and replace NAVIGATE URL | Institution endpoint and selector differences are embedded in copied artifact | Run Alpha/Beta separately with different databases/branding/DOM; resolve endpoint and overlays from trusted runtime config while artifact remains byte-identical | Same digest/artifact executes against both independent targets; state isolation and drift evidence | 1, 25-26 | FIXED | Alpha and Beta run as distinct FastAPI services on ports 8001/8002 with separate SQLite databases and materially different branding/DOM. Trusted routing selects endpoint/overlay from institution input; one unchanged, hash-verified artifact executes against both and each target store contains only its own effect. | pending |
| H-09 | Append-only ledger claim is false | HIGH | Mutable cases/executions/intents/deadlines/leases; cascade deletes in `models.py` | Mutable projections are treated as authoritative history and audit events are deletable | Add immutable sequenced hash-chained events as authority; prohibit mutation/deletion/cascade of audit facts; derive projections | Tamper-chain detection; append-only enforcement; delete/recreate projections then `rebuild_case` equivalence | 1, 16-17 | FIXED | 8/8 event-ledger tests pass: database triggers reject event UPDATE/DELETE, case deletion cannot cascade history, hash verification detects privileged tampering, 30 concurrent appends produce a continuous sequence, and destroyed mutable projections rebuild equivalently without appending history. Impacted ledger/workflow/crash suites pass 3/3; scoped Ruff and mypy pass. | `d2cbdc2`, `f411f92` |
| H-10 | Operator and simulator mutations are unauthenticated | HIGH | Services bind `0.0.0.0`; reset/mode/credit/notice/lease routes lack auth/CSRF | Demo administration and application routes share an open trust boundary | Bind localhost by default; token-authenticate mutation/admin/operator APIs; CSRF-protect browser forms; isolate test administration | Unauthenticated/invalid-token/CSRF requests rejected; valid scoped operations accepted; bind-default test | 1, 30 | PLANNED | Baseline pending | - |
| H-11 | CI clean sync cannot execute its gate | HIGH | CI uses `uv sync`; pytest is only in optional `dev`; no Ruff/mypy | Setup instructions and CI dependency/gate definitions are incomplete | Use frozen dev sync; pin CI tool/action majors; gate Ruff, mypy, coverage, all tests, build, wheel smoke, browser E2E | Clean checkout CI-equivalent script and workflow validation | 1, 32, 34-35, 42 | PLANNED | Baseline pending | - |
| H-12 | Dockerfile fails before source is copied | HIGH | `uv sync --frozen` runs with only `pyproject.toml`/lock and missing README; Compose file bind can become directory | Docker layer ordering attempts editable project install before build inputs; unsafe file bind mount | Install dependencies without project before source copy, then install project; use named volume or mounted directory | Static Dockerfile/Compose test and real build when daemon is available | 1, 33, 42 | PLANNED | Baseline pending | - |
| M-01 | Monetary logic uses binary floats and accepts non-finite values | MEDIUM | Float models/columns; `float("nan")` bypasses policy | No canonical money type or finite/currency-scale validation exists | Introduce finite, cent-quantized `Decimal` money type across schemas, policy, DBs, simulators, and effect comparison | Boundary table: 0, .01, 340, 499.99, 500, 500.01, huge, NaN, +/-Infinity; no core-path float conversion | 1-2 | FIXED | `40 passed` across affected unit/integration/audit/regression paths; no monetary `Float`/business `float()` remains | `8370996` |
| M-02 | Deadline model omits holidays/timezones/90-day/overdue | MEDIUM | `deadlines.py:7-21` skips weekends only and fixes 17:00 UTC | Deadline function has no calendar provider, injected clock, institution timezone, or lifecycle evaluation | Add configurable rules/calendar, US federal holidays, timezone/clock injection, month/year/weekend behavior, 45/90-day cases, and overdue transitions | Deterministic holiday/weekend/month/year/timezone/overdue deadline tests | 1, 14, 28 | PLANNED | Baseline pending | - |
| M-03 | Evidence/procedure abstractions are decorative | MEDIUM | Unused `ProcedureDefinition`, `EffectEvidenceRecord`, `capture_evidence`, templates, session ID, reconciliation settings/outcomes | Schema/design objects were created without runtime integration | Wire evidence, provenance, session identity, and reconciliation policy into runtime; remove remaining unused abstractions/actions/metadata | Runtime evidence/provenance/session/reconciliation tests plus static unused-path review | 1, 16-20, 24, 36 | PARTIALLY FIXED | Discovery evidence/provenance and durable browser-session identity now participate in runtime paths; remaining decorative abstractions are scheduled for Phase 36 | `a79e6fe`, `ffd9650` |

## Baseline

Baseline commands and results will be recorded here before Phase 2 implementation begins.

| Command | Result | Notes |
|---|---|---|
| `uv sync --extra dev --frozen` | PASS | 43 packages checked; exit 0 |
| `uv run pytest tests -q` | PASS | 45 passed, 1 Starlette/httpx deprecation warning; 149.58s |
| `uv run pytest audit_tests -q` | FAIL | 10 failed out of 10; 24.10s. Failures cover artifact integrity/schema, guard evidence/case/value binding, precheck outage, lease overwrite, NaN, target restart, and duplicate workers. |
| `uv run ruff check .` | FAIL | 52 errors; 50 reported automatically fixable |
| `uv run mypy tandem simulators` | FAIL | 8 errors in 3 files; checked 50 source files |
| `uv run pytest tests/regression -q` | FAIL (EXPECTED RED BASELINE) | 31 failed, 7 passed; 46.56s. New contracts cover all Phase 1 minimum categories through the matrix in `tests/regression/REQUIREMENTS_MATRIX.md`. |

Baseline recorded before any production-code remediation. The baseline preserves the distinction between currently passing happy-path coverage and failing adversarial safety behavior.

## Status Vocabulary

- `PLANNED`: root cause, fix, regression, and phase mapped; production implementation not started.
- `IN PROGRESS`: implementation has begun but all closure evidence is not yet present.
- `FIXED`: implementation and all relevant regression/broader verification have passed.
- `PARTIALLY FIXED`: a documented subset is verified and an explicit remainder exists.
- `INTENTIONALLY DE-SCOPED`: product claim was removed and the prototype boundary is explicit.
- `NOT FIXED`: attempted or re-evaluated and still fails.
